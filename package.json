{
  "name": "markethub",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.8.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "cd apps/web && bun run dev",
    "dev:api": "cd apps/api && bun run dev",
    "db:generate": "bun --cwd packages/shared/src/db drizzle-kit generate",
    "db:migrate": "bun --cwd packages/shared/src/db drizzle-kit migrate"
  },
  "dependencies": {
    "@elysiajs/eden": "^1.4.9"
  }
}
